/**
 * Generate an array of canvas radialGradients.
 * @param color The foreground color to use.
 * @returns An array of 4 gradients. (add background yourself)
 */
export function generateUltrablurCanvas(color: string) {}
